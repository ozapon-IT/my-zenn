# 執筆規約

このリポジトリで AI に記事作成を手伝ってもらう際のルールです。

## 記事タイプ

記事は以下の 2 タイプに分類し、それぞれのテンプレートと構成に従う。

| タイプ | 目的 | 想定読者 | テンプレート |
|--------|------|----------|--------------|
| トラブルシュート型 | 検索流入の獲得 | 同じ問題でハマって検索してきたエンジニア | `templates/article-troubleshoot.md` |
| アーキテクチャ・ロジック解説型 | 設計力・思考の言語化(資産記事) | 同種のシステムを設計・実装しようとしているエンジニア | `templates/article-architecture.md` |

- 解説型を「幹」、トラブルシュート型を「枝葉」と位置づけ、関連する記事同士は本文中で相互リンクする
- 新規記事の作成時は、まずどちらのタイプかを明確にしてからテンプレートを選ぶ

## 共通の執筆スタイル

- 結論ファースト。冒頭に要約(TL;DR)を置く
- 実際に動かしたコード・実際のエラーメッセージ・実際の設計判断を載せる(一次情報を重視)
- 過度な煽りタイトル禁止。「〜の罠」「〜でハマった話」「〜の設計と実装」程度は OK
- 文体は「です・ます」調
- 図が有効な箇所(構成図・シーケンス・状態遷移)は Mermaid 記法で描く

### 外部情報への言及(公式ドキュメント・他者の記事・OSS等)

- 公式ドキュメント・他社サービス・他者の記事・OSS を悪く書かない。「不親切」「不備」「分かりにくい」等の評価語を使わない
- ハマった話は自分の実装・設計変更・読み落としを主語にして書く(「ドキュメントのせい」ではなく「自分はこう踏んだ」)
- 記載の不足に触れる場合は、評価ではなく検証済みの事実として書く。「〜への言及はない(YYYY年M月時点で確認)」の形にし、必ず裏どり(原文引用・実測)を添える
- 公式の記載・サンプルどおりで正しく動くケースがあるなら、それをフォローとして明記する(例: 「公式サンプル通りに実装すれば踏まない」)

### 読者レベルと説明の粒度

- 想定読者は実務2〜3年目のエンジニア。一般的な技術用語(AWSサービス名、HMAC、CI/CD等)は説明しない
- 本記事・本システム固有の概念(独自の設計・用語・ロジック)は、初出時に必ず説明する。ここは省略しない
- 迷ったら「半年前の自分が読んで詰まらないか」を基準にする
- 1つの段落は3〜4行以内に収める

## タイプ別の構成

### トラブルシュート型

「何が起きて、原因は何で、どう解決するか」を最短で伝える。

構成: TL;DR → 背景 → 問題 → 原因 → 解決策 → まとめ

### アーキテクチャ・ロジック解説型

「なぜこの設計にしたか」の判断過程を主役にする。結果だけでなく、検討した代替案と採用しなかった理由を必ず含める。

構成: TL;DR → 前提(システム概要・制約条件) → 設計の全体像(図) → 各要素の解説 → 設計判断(代替案との比較・トレードオフ) → まとめ

- コードは全量を貼らず、設計意図が伝わる最小限の抜粋にとどめる
- ドメインロジック(計算式・アルゴリズム)を扱う場合は、コードと数式・図を併用して説明する

## FX/トレード関連記事の制約(重要)

- 損益・運用成績・利回りには一切言及しない
- 投資助言と誤解される表現(「儲かる」「勝てる」等)を使わない
- あくまでアーキテクチャ・実装の技術記事として書く
- 解説型でトレードロジック(エントリー条件・stop_loss 算出等)を扱う場合も、「実装・設計の解説」に徹し、手法の有効性・優位性を主張しない

## セキュリティ(公開前チェックリスト)

- API キー、シークレット、アカウント ID、口座情報が含まれていないか
- 実コードを引用する際は認証情報部分をプレースホルダに置換しているか
- 個人情報(住所・電話番号等)が含まれていないか
- 構成図・スクリーンショットに AWS アカウント ID・ARN・エンドポイント URL 等が写り込んでいないか

## 運用ルール

- 下書きは `published: false` のまま作業ブランチで書き、公開時のみ main に push する
- `topics` は 4 つ以内で、検索されやすい一般的なスラッグを使う(例: `aws`, `lambda`, `typescript`, `fx`)
